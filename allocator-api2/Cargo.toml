[package]
name = "allocator-api2"
version = "0.3.0"
edition = "2018"

[dependencies]
console = { path = "../console", features = ["usb_uart"]}
defmt = { version = "0.3.10", optional = true }

[features]
alloc = []
