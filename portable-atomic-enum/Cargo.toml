[package]
name = "portable-atomic-enum"
version = "0.3.1"
edition = "2021"

[workspace]
members = [
    ".",
    "./macros"
, "macros"]


[dependencies]
portable-atomic = { path = "../portable-atomic", default-features = false, optional = true }
portable_atomic_enum_macros = { path = "./macros" }

[features]
portable-atomic = ["dep:portable-atomic"]
