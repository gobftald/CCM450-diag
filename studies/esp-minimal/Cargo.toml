[package]
name = "esp-minimal"
version = "0.1.0"
edition = "2021"

[dependencies]
esp-hal          = { path = "../../esp-hal/esp-hal" }
defmt            = { version = "0.3.10"}

[features]
default = ["esp32c3"]

esp32c3 = ["esp-hal/esp32c3"]
