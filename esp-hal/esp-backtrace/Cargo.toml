[package]
name = "esp-backtrace"
version = "0.16.0"
edition = "2024"

[dependencies]
console  = { path = "../../console", features = ["jtag_serial"] }      # default console
defmt    = { version = "0.3.10", optional = true }
esp-config  = { version = "0.4.0", path = "../esp-config" }
heapless = "0.8"

[features]
default = ["esp32c3"]

esp32c3 = []
defmt   = ["dep:defmt", "console/defmt"]

panic-handler     = []
exception-handler = []
